# SweetPad 运行指南 🍬

## 🚀 快速开始

### 1️⃣ 安装 SweetPad

在 VS Code 中安装扩展：
```
1. 打开 VS Code
2. 按 ⌘ + Shift + X 打开扩展面板
3. 搜索 "SweetPad"
4. 点击 "Install"
```

或者通过命令行：
```bash
code --install-extension sweetpad.sweetpad
```

---

### 2️⃣ 安装依赖

确保你的 Mac 已安装：

```bash
# 检查 Xcode
xcode-select --install

# 检查 Swift
swift --version

# 安装 Xcode Command Line Tools（如果还没有）
xcode-select --install
```

---

### 3️⃣ 打开项目

```bash
cd "/Users/mac/iCloud Drive (Archive) - 1/Documents/Documents - bluerose/Demohive/gayish"
code .
```

---

### 4️⃣ 配置 SweetPad

项目已经配置好了！查看 `.vscode/settings.json`：

```json
{
  "sweetpad.selectedDevice": "iPhone 15 Pro",
  "sweetpad.selectedScheme": "Gayish",
  "sweetpad.buildConfiguration": "Debug",
  "sweetpad.autoShowSimulator": true
}
```

---

### 5️⃣ 运行项目

#### 方式一：使用命令面板（推荐）

```
1. 按 ⌘ + Shift + P
2. 输入 "SweetPad: Run"
3. 选择 "iPhone 15 Pro"
4. 等待构建和启动
```

#### 方式二：使用侧边栏

```
1. 点击左侧 SweetPad 图标
2. 选择设备：iPhone 15 Pro
3. 点击 "Run" 按钮 ▶️
```

#### 方式三：使用快捷键

```
⌘ + R  →  运行项目
⌘ + .  →  停止运行
⌘ + B  →  仅构建
```

---

## 📱 选择设备

### 查看可用设备

```
⌘ + Shift + P → "SweetPad: Select Device"
```

推荐设备：
- ✅ iPhone 15 Pro (iOS 17.0+)
- ✅ iPhone 15 Pro Max
- ✅ iPhone 14 Pro
- ⚠️ 需要 iOS 17.0+ 系统

---

## 🔧 常用 SweetPad 命令

在 VS Code 命令面板（⌘ + Shift + P）中：

| 命令 | 说明 |
|------|------|
| `SweetPad: Run` | 运行项目 |
| `SweetPad: Build` | 构建项目 |
| `SweetPad: Clean` | 清理构建缓存 |
| `SweetPad: Select Device` | 选择模拟器 |
| `SweetPad: Show Simulator` | 显示模拟器 |
| `SweetPad: Kill Simulator` | 关闭模拟器 |

---

## 🐛 调试

### 设置断点

```
1. 在代码行号左侧点击，添加红点断点
2. 运行项目（⌘ + R）
3. 触发断点时会自动暂停
4. 使用调试工具栏：
   - Continue (F5)
   - Step Over (F10)
   - Step Into (F11)
   - Step Out (Shift + F11)
```

### 查看日志

SweetPad 会自动显示：
- ✅ 构建日志
- ✅ 运行时 print() 输出
- ✅ 错误信息

在底部的 "OUTPUT" 面板查看。

---

## 📦 项目结构（SweetPad 视角）

```
gayish/
├── .vscode/
│   ├── settings.json       # SweetPad 配置
│   └── launch.json         # 调试配置
├── GayishApp/              # 源代码目录
│   ├── GayishApp.swift     # App 入口
│   ├── Models/
│   ├── Views/
│   ├── ViewModels/
│   ├── Services/
│   └── Utilities/
├── Package.swift           # Swift Package 配置
└── Info.plist              # App 配置
```

---

## ⚡ 性能优化

### 加快构建速度

1. **增量构建**（默认开启）
   - SweetPad 自动检测改动，只编译修改的文件

2. **并行编译**
   ```json
   // .vscode/settings.json
   {
     "sweetpad.buildParallel": true
   }
   ```

3. **清理缓存**（如果构建出错）
   ```
   ⌘ + Shift + P → "SweetPad: Clean"
   ```

---

## 🎯 第一次运行步骤（完整版）

### Step 1: 打开项目
```bash
cd "/Users/mac/iCloud Drive (Archive) - 1/Documents/Documents - bluerose/Demohive/gayish"
code .
```

### Step 2: 等待 SweetPad 加载
- VS Code 底部会显示 "Indexing..."
- 等待索引完成（约 30 秒）

### Step 3: 选择设备
```
⌘ + Shift + P → "SweetPad: Select Device" → "iPhone 15 Pro"
```

### Step 4: 运行
```
⌘ + R
```

### Step 5: 等待构建
```
第一次构建约 2-3 分钟
后续增量构建约 10-30 秒
```

### Step 6: 模拟器启动
- 自动打开 iOS 模拟器
- 自动安装并启动 App
- 🎉 开始测试！

---

## 🔥 热重载（Hot Reload）

SweetPad 支持有限的热重载：

### UI 修改
- 修改 SwiftUI 视图代码
- 保存文件（⌘ + S）
- SweetPad 会尝试重新注入代码

### 完整重新运行
```
⌘ + Shift + P → "SweetPad: Run"
```

---

## 🆚 SweetPad vs Xcode

| 特性 | SweetPad | Xcode |
|------|----------|-------|
| 启动速度 | ⚡⚡⚡ 快 | 🐌 慢 |
| 内存占用 | 💚 低 | 🔴 高 |
| 代码编辑 | VS Code 强大 | 一般 |
| UI 预览 | ❌ 不支持 | ✅ SwiftUI Preview |
| 调试功能 | ✅ 基础调试 | ✅ 完整调试 |
| 性能分析 | ❌ | ✅ Instruments |
| 界面构建器 | ❌ | ✅ Interface Builder |

**推荐使用场景：**
- ✅ 日常开发：SweetPad
- ✅ UI 设计：Xcode
- ✅ 性能优化：Xcode

---

## 🐛 常见问题

### 问题 1：找不到模拟器
**解决方案：**
```bash
# 打开模拟器
open -a Simulator

# 或使用 xcrun
xcrun simctl list devices
```

### 问题 2：构建失败
**解决方案：**
```
1. ⌘ + Shift + P → "SweetPad: Clean"
2. 重新运行
3. 如果还失败，检查代码是否有语法错误
```

### 问题 3：模拟器不显示
**解决方案：**
```
⌘ + Shift + P → "SweetPad: Show Simulator"
```

### 问题 4：权限问题
**解决方案：**
```bash
# 给予 VS Code 权限
sudo xcode-select --switch /Applications/Xcode.app
```

---

## 📸 使用截图

### 运行项目
```
┌─────────────────────────────────────┐
│ VS Code                             │
│ ┌─────────────────────────────────┐ │
│ │ ⌘ + Shift + P                   │ │
│ │ > SweetPad: Run                 │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 📱 iPhone 15 Pro                │ │
│ │ 📱 iPhone 15 Pro Max            │ │
│ │ 📱 iPhone 14 Pro                │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 侧边栏
```
┌──────────────────┐
│ 🍬 SWEETPAD      │
│                  │
│ 📱 Devices       │
│  ├─ iPhone 15 Pro│
│  └─ iPhone 14 Pro│
│                  │
│ 🎯 Actions       │
│  ├─ ▶️ Run       │
│  ├─ 🔨 Build     │
│  └─ 🧹 Clean     │
└──────────────────┘
```

---

## 🎓 学习资源

### SweetPad 官方
- 📖 文档：https://github.com/sweetpad-dev/sweetpad
- 💬 Discord：https://discord.gg/sweetpad
- 🐛 Issues：https://github.com/sweetpad-dev/sweetpad/issues

### Swift/SwiftUI
- 📖 Swift 官方文档
- 📺 SwiftUI 教程
- 🎓 Apple Developer

---

## ✅ 检查清单

运行前确保：
- [ ] 已安装 SweetPad 扩展
- [ ] 已安装 Xcode Command Line Tools
- [ ] 项目目录正确
- [ ] VS Code 已打开项目文件夹
- [ ] 已选择模拟器设备

---

## 🎉 现在开始运行！

```bash
# 1. 打开项目
cd "/Users/mac/iCloud Drive (Archive) - 1/Documents/Documents - bluerose/Demohive/gayish"
code .

# 2. 在 VS Code 中按 ⌘ + R

# 3. 等待构建和启动

# 4. 开始测试 Gayish App！
```

---

**祝你开发愉快！** 🌈✨
